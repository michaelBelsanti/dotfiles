## APPLICATIONS ##

	~/.scripts/albert.sh

# rofi
super + {space,Tab}
	rofi -show {drun,window} -theme ~/.config/rofi/main.rasi
super + m 
	rofi -show calc -theme ~/.config/rofi/calc.rasi

super + {o,g}
	~/.config/rofi/{options.sh,games.sh}
	
# term
super + Return
	alacritty

# browser 
super + b
	librewolf

# files
super + n
	nemo

# editor
super + e
	vscodium

# discord
super + d
	firejail discord

# screenshots
super + {ctrl + s, s}
	flameshot {screen -c,gui}

# xkill
super + shift + q
	xkill

# restart easyeffects
super + shift + ctrl + e
	~/.scripts/eerestart.sh

# toggle picom
super + c
	~/.scripts/picom.sh

# lock
super + shift + Delete
	betterlockscreen -l
